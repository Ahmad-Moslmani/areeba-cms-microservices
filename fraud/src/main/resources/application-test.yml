spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/fraud_db?currentSchema=fraud_test
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:sa}
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: fraud_test